<script setup>
  // not sure why this is here, but the core functions are put under the /app folder, so we can put other information pages in the base domain
  // maybe someday
  await navigateTo("/app");
</script>
<template>
  <p>Redirecting...</p>
</template>
